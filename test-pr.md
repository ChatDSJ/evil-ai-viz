# Test PR

This file was created to test the Claude Code PR workflow.

It verifies that Claude can:
- Commit changes to a feature branch
- Push the branch to origin
- Create a pull request via `gh pr create`
